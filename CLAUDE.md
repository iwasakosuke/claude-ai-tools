# このリポジトリでの作業ルール

このリポジトリは業務資産(Skill・テンプレート・SOP・QA)の保管庫です。Claude Codeはここで作業するとき、以下を守ってください。

## 最優先ルール(docs/02_運用ルール.md の要約)
- メール送信 / SNS・外部への投稿 / 外部サービスへのデータ送信 / 課金が発生する操作 / 既存ファイルの上書き・削除 は、**内容を提示して承認を得るまで実行しない**
- 認証情報をコード・ログ・コミットに含めない
- Drive/Notion/デザインへの保存は常に「新規作成」。既存の上書きは人間が判断する

## 作業の進め方
- 依頼が来たら、まず `.claude/skills/` の該当Skillに従う。該当がなければ通常対応し、くり返しそうな作業なら「Skill化しますか?」と提案する
- 成果物は対応するテンプレ(`templates/`)の形式で作り、対応するQA(`checklists/`)を自分で通して結果を添える
- ファイル名は `YYYYMMDD_種別_案件名_v番号` 形式。旧版は消さない
- 不明点で作業を止めない。仮説を置いて進め、仮説には必ず **【仮説】** と明記する
- 人間にしか決められない箇所(金額・可否・公開判断)は **【要判断】** プレースホルダにする

## 資産の改善
- 出力が手で直されたら、直しをテンプレ/Skillへの反映として提案する
- 資産の変更コミットは `improve: <対象> <理由>` 形式。効果は `docs/03_効果ログ.md` に追記(過去行の書き換え禁止)
